{% extends 'base.html.twig' %}

{% block body %}
    <h1 style="margin: 80px 0 20px;">Film creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}<br>
        <input type="submit" class="btn btn-success" value="Create" />
    {{ form_end(form) }}<br>
    
            <a href="{{ path('film_index') }}"  class="btn btn-primary">Back to the list</a>
     
{% endblock %}
